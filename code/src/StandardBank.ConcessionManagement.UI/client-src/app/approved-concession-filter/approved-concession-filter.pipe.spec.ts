import { ApprovedConcessionFilterPipe } from './approved-concession-filter.pipe';

describe('ApprovedConcessionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ApprovedConcessionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
