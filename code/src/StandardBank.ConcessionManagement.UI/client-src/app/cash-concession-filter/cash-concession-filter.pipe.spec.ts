import { CashConcessionFilterPipe } from './cash-concession-filter.pipe';

describe('CashConcessionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CashConcessionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
