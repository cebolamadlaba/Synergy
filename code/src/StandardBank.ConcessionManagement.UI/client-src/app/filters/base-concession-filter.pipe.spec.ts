import { BaseConcessionFilterPipe } from './base-concession-filter.pipe';

describe('BaseConcessionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BaseConcessionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
