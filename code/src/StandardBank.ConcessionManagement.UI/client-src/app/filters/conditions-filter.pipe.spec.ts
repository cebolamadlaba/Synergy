import { ConditionsFilterPipe } from './conditions-filter.pipe';

describe('ConditionsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ConditionsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
