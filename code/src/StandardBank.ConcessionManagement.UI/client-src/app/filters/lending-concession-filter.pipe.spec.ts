import { LendingConcessionFilterPipe } from './lending-concession-filter.pipe';

describe('LendingConcessionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LendingConcessionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
